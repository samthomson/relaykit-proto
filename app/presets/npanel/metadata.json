{
  "id": "npanel",
  "name": "nPanel",
  "label": "nPanel",
  "repo": "https://github.com/hzrd149/nsite-gateway",
  "icon": "◈",
  "description": "nPanel: nsite gateway + built-in NIP-05 names on the same domain.",
  "version": "1.0.0",
  "type": "npanel",
  "serviceName": "npanel-caddy",
  "internalPort": 8080,
  "domainConfigKey": "NSITE_ROUTER_HOST",
  "requiredConfig": [
    {
      "id": "NSITE_PARENT_DOMAIN",
      "name": "Site domain",
      "type": "string",
      "required": true,
      "description": "Per deployment — whatever comes after the long site label in the URL. Examples: relayk.it for one gateway, testsub.example.com for another. RelayKit builds the full host as <npub or base36+d>.<this>. Not a global setting: each nPanel service picks its own.",
      "placeholder": "relayk.it or testsub.example.com"
    },
    {
      "id": "NSITE_SITE_NPUB",
      "name": "Publishing key",
      "type": "string",
      "required": true,
      "description": "Hex pubkey (64 hex chars) or npub1… for the account that signs manifests in Shakespeare. Same account for all sites under that key; each site is distinguished by the site id below.",
      "placeholder": "npub1… or hex"
    },
    {
      "id": "NSITE_SITE_D",
      "name": "Site id (d tag)",
      "type": "string",
      "required": false,
      "description": "Named site identifier for kind 35128 (Shakespeare). Leave empty only if you publish a root manifest (kind 15128). Must match NIP-5A: 1–13 chars [a-z0-9-], must not end with -.",
      "placeholder": "myblog — empty for root-only 15128"
    },
    {
      "id": "NSITE_VISITOR_HOST",
      "name": "Public hostname (optional)",
      "type": "string",
      "required": false,
      "description": "What visitors type and DNS should point at (e.g. relayk.it). Leave empty to use only the long NIP-5A hostname. RelayKit registers routing and TLS for this host when you deploy or Save + Redeploy.",
      "placeholder": "empty = use computed NIP-5A host"
    },
    {
      "id": "NPANEL_NIP05_USERS",
      "name": "NIP-05 users",
      "type": "string",
      "required": false,
      "description": "Comma or newline separated name=npub|hex mappings (e.g. sam=npub1...,alex=hex...).",
      "placeholder": "sam=npub1..."
    },
    {
      "id": "NOSTR_RELAYS",
      "name": "Event / manifest relays",
      "type": "string",
      "required": false,
      "description": "Comma-separated wss:// URLs used to load events and sync manifests. 2-5 is plenty: the relays you publish your manifest to plus 1-2 ecosystem defaults (nsite.run, purplepag.es). More relays = slower scans on every gateway cold start.",
      "placeholder": "wss://…"
    },
    {
      "id": "LOOKUP_RELAYS",
      "name": "Discovery relays",
      "type": "string",
      "required": false,
      "description": "Comma-separated wss:// URLs used to find routing and Blossom hints for a pubkey. Profile-hint relays like purplepag.es cover most cases; 1-3 is enough.",
      "placeholder": "wss://…"
    },
    {
      "id": "BLOSSOM_SERVERS",
      "name": "Blob mirror URLs",
      "type": "string",
      "required": false,
      "description": "Comma-separated https:// Blossom bases when the manifest does not list servers.",
      "placeholder": "https://…"
    }
  ]
}

{
  "version": "0.3.1",
  "dokployVersion": "0.30.2",
  "notes": "fix: services could silently lose ssl (traefik default cert, never healed on redeploy). domain rows now reconcile with configured hosts on every deploy, config save and domain edit — stale hosts or missing letsencrypt are deleted and re-registered."
}

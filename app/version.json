{
  "version": "0.3.2",
  "dokployVersion": "0.30.2",
  "notes": "routing health tooling: debug page shows ghost routers (stale traefik labels on running containers) with one-click hard reset, plus a dns check across all domains (flags unresolvable, dotless, cloudflare-proxied hosts). invalid hostnames are rejected at deploy/save. the tls retry now waits for the proxy restart behind a reconnect modal instead of hanging the dashboard."
}
